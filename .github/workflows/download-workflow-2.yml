name: Download Workflow 1
on: [push]
jobs:
  download-job:
    runs-on: ubuntu-latest
    steps:
      - name: Download a single artiface
        uses: actions/download-artifact@v3
        with:
          name: output-lof-file
